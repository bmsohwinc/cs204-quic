hosts:
  h1:
    type: local          # or docker, ssh (for future)
    ip: localhost
    ssh: null            # e.g., "user@cloudlab-node" in future

  h2:
    type: local
    ip: localhost
    ssh: null

links:
  lo:
    type: local_iface    # or docker_network (future)
    iface: lo
    description: localhost loopback

  # example future docker bridge
  # bridge0:
  #   type: docker_network
  #   iface: br-quic
  #   description: Docker bridge for QUIC tests
  